import org.w3c.dom.ls.LSInput

class Q13 {
    static void main(String[] args) {
        String s="Consider a class named \"Stack\" that holds a list of objects and has the following operations associated: "

        List l=s.trim().split(" ").join("").split("")
        println(l)
        println(Collections.frequency(l,'a'))
    }
}
